# @ai-sales/design-tokens

Semantic design tokens (color, typography, spacing, radius, shadow, z-index, motion, breakpoint,
density — spec 7.1). The README is the source of truth: the current palette is an explicit
**PROVISIONAL** placeholder pending a real Figma/brand handoff (spec 7.7) — don't treat its values
as final, and when real tokens land, only `src/tokens.ts` should need to change since every
consumer references semantic names, never raw hex values.

@README.md
