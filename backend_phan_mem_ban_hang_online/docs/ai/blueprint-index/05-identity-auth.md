# Blueprint §5 — Identity and auth

**Source:** §5.1–5.6 (search `# 5.`)

- Identity, tenant, membership, role, session, device models.
- JWT access + refresh rotation, MFA, tenant switch, support access grants.
- Permission naming: `<resource>.<action>` from permission matrix.
- Related ADRs: ADR-008 (JWT refresh).
