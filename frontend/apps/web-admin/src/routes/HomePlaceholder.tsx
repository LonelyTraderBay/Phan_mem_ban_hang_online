// Lazy-loaded placeholder route proving the code-splitting/route-manifest wiring (FE-F00-002
// step 3). Replaced by a real feature route as business features land (F01+).
export default function HomePlaceholder() {
  return (
    <div style={{ padding: 24 }}>
      <h1>AI Sales OS — Web Admin</h1>
      <p>Nền tảng kỹ thuật (F00) đã sẵn sàng.</p>
    </div>
  );
}
