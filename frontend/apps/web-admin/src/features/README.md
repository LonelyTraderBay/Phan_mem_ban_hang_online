# Features

Empty in F00 except for one sample feature added in CP8 to prove the layer/import-boundary
chain (F00.6 exit criterion). Each feature follows the structure in spec section 4.2:
`api/`, `components/`, `domain/`, `hooks/`, `routes/`, `schemas/`, `state/`, `tests/`, `index.ts`,
`README.md`.
